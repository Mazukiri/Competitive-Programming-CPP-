#include <bits/stdc++.h>
using namespace std;

signed main(){
    
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    if (fopen("dp.inp", "r")) {
        freopen("dp.inp", "r", stdin);
        freopen("dp.out", "w", stdout);
    }

    //int A[1001][1001];
    //int A[2000001]; 

    //int ans = 0;

    //for (int i = 1; i<=2000000; i++) A[i] = 1;
    
    //for (int i = 1; i<=2000000; i++) if (A[i] == 1) ans++;

    //cout << ans;

}