#include <bits/stdc++.h>
using namespace std;
#define ll long long
const int N = 1e6 + 5;

signed main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    if (fopen("_ab.inp", "r")) {
        freopen("_ab.inp", "r", stdin);
        freopen("_ab.out", "w", stdout);
    }

    dp[1] = 2;
    dp[2] = 8;
    

    for (int i = 2; i <= 6; i++) {
        dp[i] = (dp[i]) 
        cout << dp[i] 
    }

    int t; cin >> t;

    while (t--) {
        int x; cin >> x;

    }
}